+ interactive du (table, preload (save state) and update per dir)
+ color palette browser (named color, palette, rgb, ...)
  https://github.com/joowani/colorpedia
+ menu object and instance command
+ track state (focus, position,..) and allow query of current value ...
+ tab focus between objects (test with two menus)
+ more commands
+ support menu (current size and scrollbar)
+ infrastructure for menu builtin
+ multi-column output
